coordinates = (4, 5) #tuples
#tuples are immutable cannot be changed

print(coordinates[0])

#coordinates[1] = 45 #does not work because it is immutable
[(1, 2), (3, 4), (5, 6)] #tuples cannot be changed even in a list
